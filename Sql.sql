### Source Table:
CREATE TABLE TEST
(
ID INT UNIQUE IDENTITY(1,1),
NAME NVARCHAR(25),
CREATEDDATE DATETIME,
UPDATEDATE DATETIME
)

### Destination,Dump Table:
CREATE TABLE TEST
(
ID INT UNIQUE,
NAME NVARCHAR(25),
CREATEDDATE DATETIME,
UPDATEDATE DATETIME
)


### Source Trigger:

CREATE TRIGGER UPDATEDATETRIGGER
ON TEST
AFTER UPDATE
AS
UPDATE TEST
SET UPDATEDATE=GETDATE()
WHERE ID IN (SELECT ID FROM inserted)


### MAX DATE:
SELECT MAX(ISNULL(FORMAT(UPDATEDATE,'yyyy-MM-dd HH:mm:ss:s'),FORMAT(CREATEDDATE,'yyyy-MM-dd HH:mm:ss:s'))) FROM TEST 


### Update SQL CODE:
UPDATE TEST2 
SET NAME=TD.NAME,
UPDATEDATE=TD.UPDATEDATE
FROM TEST2 TS
INNER JOIN TEST2DUMP TD
ON TS.ID= TD.ID
